# Clothing application service

This directory mimics the existing clothing application. It owns SQLAlchemy
catalog retrieval and temporary cart APIs. Use the workspace-level
`README.md`, `.env`, `requirements.txt`, and `.venv` to run it.

The service does not create, migrate, reset, or seed PostgreSQL.
